#include "mempool.h"
#include "tx.h"
#include <cassert>

int main(){
    // create tx below min relay fee -> rejected
    // create tx with non-final -> rejected
    // valid tx -> accepted
    return 0;
}
